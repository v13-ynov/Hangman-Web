{
  "configurations": [
      {
          "type": "chrome",
          "request": "launch",
          "name": "Lancer Firefox",
          "runtimeExecutable": "/Applications/Firefox.app/Contents/MacOS/firefox",
          "url": "http://localhost:3000",  // Modifiez l'URL si nécessaire
          "webRoot": "${workspaceFolder}"
      }
  ]
}
